{
    "pugi::xpath_variable_set::get": "For `const xpath_variable* get(const char_t* name) const;`, use `const pugi::xpath_variable_set variables_const; const pugi::xpath_variable* var_const = variables_const.get(\"variable_name\");`",
    "pugi::xml_attribute::set_name": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_attribute::set_value": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::child": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::attribute": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::next_sibling": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::previous_sibling": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::set_name": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::set_value": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::append_attribute": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::prepend_attribute": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::insert_attribute_before": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::append_child": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::prepend_child": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::insert_child_before": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::remove_child": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::insert_child_after": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::remove_attribute": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_text::set": "For `string_view_t` parameters, use `std::string_view a_str;` to initialize.",
    "pugi::xml_node::insert_attribute_after": "For `string_view_t` parameters, use`std::string_view a_str;` to initialize.",
    "pugi::xml_document::load_file": "For `options` parameter, initialize with random integer. For `encoding` parameter, initialize with random value of `enum xml_encoding`.",
    "pugi::xml_document::reset": "For `void reset(const xml_document& proto);`, do pass the `xml_document` object as a parameter."
}